name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

  test:
    name: Test (Rust ${{ matrix.toolchain }})
    runs-on: ubuntu-latest
    env:
      RUSTUP_TOOLCHAIN: ${{ matrix.toolchain }}
    strategy:
      fail-fast: false
      matrix:
        toolchain: ["1.85", stable]
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.toolchain }}

      - uses: Swatinem/rust-cache@v2

      - name: Check
        run: cargo check --all-targets --all-features

      - name: Test
        run: cargo test --all

      - name: Test with serde
        run: cargo test --features serde

      - name: Doc tests
        run: cargo test --doc --all-features

  audit:
    name: Security audit
    runs-on: ubuntu-latest
    env:
      RUSTUP_TOOLCHAIN: stable
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: rustsec/audit-check@v2
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

  python:
    name: Python
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.9", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install maturin and pytest
        run: pip install maturin pytest

      - name: Build wheel
        run: maturin build --out dist
        working-directory: bindings/python

      - name: Install wheel
        run: pip install --no-index --find-links dist nidx
        working-directory: bindings/python

      - name: Run tests
        run: pytest tests/
        working-directory: bindings/python

  wasm:
    name: WASM
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: stable
          targets: wasm32-unknown-unknown

      - uses: Swatinem/rust-cache@v2

      - name: Install wasm-bindgen-cli
        run: cargo install wasm-bindgen-cli --version 0.2.113 --locked
        env:
          RUSTUP_TOOLCHAIN: stable

      - name: Build
        run: |
          cargo build --lib --target wasm32-unknown-unknown --release -p nidx-wasm
          wasm-bindgen target/wasm32-unknown-unknown/release/nidx_wasm.wasm \
            --out-dir bindings/wasm/pkg \
            --target bundler \
            --typescript

      - name: Test (Node)
        run: cargo test --target wasm32-unknown-unknown -p nidx-wasm
        env:
          WASM_BINDGEN_TEST_ONLY_NODE: 1
